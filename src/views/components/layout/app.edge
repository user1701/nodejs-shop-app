<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{{title || "Test node app"}}</title>
    @if ($slots.meta)
        {{{ await $slots.meta() }}}
    @endif

    <link rel="stylesheet" href="/css/styles.css" />
    @if ($slots.styles)
        {{{ await $slots.styles() }}}
    @endif
</head>

<body>
    @include('partials/header')
    <main class="main-content">
        <div class="container">
            {{{ await $slots.main() }}}
        </div>
    </main>

    @stack('scripts')
    @if ($slots.scripts)
        {{{ await $slots.scripts() }}}
    @endif
</body>
</html>
